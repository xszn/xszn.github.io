---
date: 2025
---

# 2025 年更新日志

## 12 月

<ul v-for="log of changelog.filter(item => 
    typeof item.frontmatter.date === 'string' &&
    item.frontmatter.date.startsWith('2025-12')
)">
    <li>
        {{ log.frontmatter.date.slice(0, 10) }}：<a :href="log.url">{{ log.url.replace(/\.html$/, '') }}</a>
    </li>
</ul>

## 11 月

<ul v-for="log of changelog.filter(item => 
    typeof item.frontmatter.date === 'string' &&
    item.frontmatter.date.startsWith('2025-11')
)">
    <li>
        {{ log.frontmatter.date.slice(0, 10) }}：<a :href="log.url">{{ log.url.replace(/\.html$/, '') }}</a>
    </li>
</ul>

<script setup>
import { data as changelog } from '../../.vitepress/theme/data/index.data.js'
</script>